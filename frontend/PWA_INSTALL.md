# 📱 Установка PWA приложения Marketing Doors

## Что такое PWA?

Progressive Web App (PWA) — это веб-приложение, которое можно установить на телефон или компьютер, как обычное приложение. После установки приложение работает офлайн, отправляет push-уведомления и выглядит как нативное приложение.

## ✅ Что уже настроено

1. **Service Worker** — для работы офлайн и кеширования
2. **Manifest** — описание приложения для установки
3. **Мета-теги для iOS** — поддержка установки на iPhone/iPad
4. **Компонент установки** — автоматическое предложение установить приложение

## 📱 Как установить приложение

### На Android (Chrome, Edge, Samsung Internet)

1. Откройте приложение в браузере
2. Появится баннер "Установить приложение" — нажмите "Установить"
   - ИЛИ нажмите меню браузера (три точки) → "Установить приложение"
   - ИЛИ нажмите кнопку "Добавить на главный экран"
3. Подтвердите установку
4. Приложение появится на главном экране

### На iPhone/iPad (Safari)

1. Откройте приложение в Safari
2. Нажмите кнопку "Поделиться" (квадрат со стрелкой вверх) внизу экрана
3. Прокрутите вниз и выберите "На экран «Домой»"
4. При необходимости измените название
5. Нажмите "Добавить"
6. Приложение появится на главном экране

**Важно для iOS:**
- Установка возможна только через Safari
- После установки приложение откроется в полноэкранном режиме (без адресной строки)

### На компьютере (Chrome, Edge, Opera)

1. В адресной строке появится иконка установки (или кнопка "+" в меню)
2. Нажмите на иконку/кнопку
3. Подтвердите установку
4. Приложение появится в меню "Пуск" / Launchpad / список приложений

## 🔧 Требования для установки

### Обязательные условия:

1. **HTTPS** — приложение должно работать по защищённому протоколу (https://)
   - В разработке (localhost) это работает автоматически
   - В продакшене нужен SSL-сертификат

2. **Service Worker** — должен быть зарегистрирован
   - Автоматически настраивается через VitePWA плагин

3. **Manifest файл** — описание приложения
   - Автоматически генерируется при сборке

4. **Иконки** — минимум 192x192 и 512x512 пикселей
   - Уже добавлены в `public/icon-192x192.png` и `public/icon-512x512.png`

### Дополнительные требования для продакшена:

1. **SSL сертификат** — обязательно для PWA
2. **Доступность по HTTPS** — все ресурсы должны загружаться по HTTPS
3. **CORS настройки** — если API на другом домене

## 🚀 Как это работает технически

### 1. Service Worker

Service Worker — это JavaScript-файл, который работает в фоне браузера:
- Кеширует ресурсы для офлайн работы
- Перехватывает сетевые запросы
- Обрабатывает push-уведомления

**Регистрация:**
- Автоматически регистрируется VitePWA плагином при запуске приложения
- Проверка: `navigator.serviceWorker.ready`

### 2. Manifest (manifest.webmanifest)

Файл описывает приложение:
- Название и описание
- Иконки разных размеров
- Цвета темы
- Режим отображения (standalone = как нативное приложение)

**Локация:**
- Генерируется автоматически при сборке
- Доступен по адресу: `/manifest.webmanifest`

### 3. Before Install Prompt

Событие, которое браузер показывает, когда приложение готово к установке:
- Автоматически перехватывается компонентом `InstallPrompt`
- Показывает пользователю предложение установить приложение
- Обрабатывает выбор пользователя (установить/отклонить)

### 4. Мета-теги для iOS

Специальные теги для поддержки установки на iOS:
```html
<meta name="apple-mobile-web-app-capable" content="yes" />
<meta name="apple-mobile-web-app-status-bar-style" content="default" />
<link rel="apple-touch-icon" href="/icon-192x192.png" />
```

## 📋 Проверка работы PWA

### В браузере (DevTools):

1. Откройте DevTools (F12)
2. Перейдите на вкладку "Application" (Chrome) или "Application" (Edge)
3. Проверьте:
   - **Manifest** — должен быть загружен и показан
   - **Service Workers** — должен быть активен (status: activated and running)
   - **Cache Storage** — должны быть кеши

### Lighthouse проверка:

1. Откройте DevTools → вкладка "Lighthouse"
2. Выберите "Progressive Web App"
3. Нажмите "Generate report"
4. Оценка должна быть ≥ 90 для PWA

### Проверка установки:

- **Android**: в адресной строке должна появиться иконка установки
- **iOS**: в Safari появится кнопка "Поделиться" → "На экран «Домой»"
- **Desktop**: в адресной строке появится иконка установки

## 🔍 Отладка

### Service Worker не регистрируется:

1. Проверьте консоль браузера на ошибки
2. Убедитесь, что приложение работает по HTTPS (или localhost)
3. Проверьте, что файл `sw.js` доступен

### Манифест не загружается:

1. Проверьте путь к манифесту в HTML: `<link rel="manifest" href="/manifest.webmanifest" />`
2. Проверьте, что манифест доступен по адресу: `https://ваш-домен/manifest.webmanifest`
3. Проверьте конфигурацию в `vite.config.ts`

### Установка не работает:

1. **Android**: убедитесь, что `beforeinstallprompt` событие перехватывается
2. **iOS**: убедитесь, что мета-теги добавлены в `index.html`
3. Проверьте, что все требования выполнены (HTTPS, manifest, service worker)

## 📝 Структура файлов

```
frontend/
├── public/
│   ├── icon-192x192.png      # Иконка 192x192
│   ├── icon-512x512.png      # Иконка 512x512
│   └── sw.js                 # Service Worker (объединяется с Workbox)
├── src/
│   ├── components/
│   │   └── common/
│   │       └── InstallPrompt.tsx  # Компонент промпта установки
│   └── ...
├── index.html                # HTML с мета-тегами для iOS
├── vite.config.ts           # Конфигурация VitePWA
└── manifest.webmanifest     # (генерируется автоматически)
```

## 🎯 Следующие шаги

1. **Тестирование на реальных устройствах:**
   - Android телефон (Chrome)
   - iPhone/iPad (Safari)
   - Desktop (Chrome/Edge)

2. **Оптимизация:**
   - Проверка Lighthouse
   - Оптимизация иконок
   - Настройка кеширования

3. **Продакшен:**
   - Настройка HTTPS
   - Проверка работы в продакшене
   - Мониторинг установок

## 📚 Дополнительные ресурсы

- [MDN: Progressive Web Apps](https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps)
- [Web.dev: PWA Checklist](https://web.dev/pwa-checklist/)
- [Vite PWA Plugin Docs](https://vite-pwa-org.netlify.app/)
- [Workbox Documentation](https://developers.google.com/web/tools/workbox)
