# 🔑 Настройка VAPID ключей для Push-уведомлений

## Что такое VAPID?

VAPID (Voluntary Application Server Identification) — это стандарт идентификации сервера при отправке Web Push уведомлений. Для работы push-уведомлений нужны два ключа:

- **Публичный ключ** — передаётся клиенту, безопасен для публикации
- **Приватный ключ** — хранится только на сервере, используется для подписи уведомлений

## 📝 Пошаговая инструкция

### Шаг 1: Установить зависимости

```bash
pip install -r requirements.txt
```

Это установит библиотеку `pywebpush` и `cryptography`, необходимые для работы с VAPID.

### Шаг 2: Сгенерировать VAPID ключи

Запустите скрипт генерации:

```bash
python3 generate_vapid_keys.py
```

Или с активацией виртуального окружения:

```bash
source venv/bin/activate  # для Linux/Mac
# или
venv\Scripts\activate  # для Windows
python generate_vapid_keys.py
```

Скрипт выведет два ключа в формате, готовом для добавления в `.env`. Пример вывода:

```
VAPID_PUBLIC_KEY=ваш_сгенерированный_публичный_ключ
VAPID_PRIVATE_KEY=ваш_сгенерированный_приватный_ключ
```

**ВАЖНО:** Скопируйте реальные ключи из вывода скрипта и добавьте их в `.env` файл!

### Шаг 3: Настроить бэкенд (.env файл)

Создайте файл `.env` в корне проекта (если его нет) и добавьте ключи:

```env
VAPID_PUBLIC_KEY=ваш_публичный_ключ
VAPID_PRIVATE_KEY=ваш_приватный_ключ
VAPID_CLAIM_EMAIL=admin@marketingdoors.local
```

**ВАЖНО:**
- `.env` файл должен быть в `.gitignore` (не коммитить ключи в репозиторий!)
- Приватный ключ — секретный, храните его в безопасности
- `VAPID_CLAIM_EMAIL` — email для идентификации сервера (можно использовать любой)

### Шаг 4: Настроить фронтенд (опционально)

Вы можете добавить публичный ключ в `frontend/.env`:

```env
VITE_VAPID_PUBLIC_KEY=ваш_публичный_ключ
```

**Примечание:** Фронтенд автоматически получает ключ с сервера через API, если он не задан в `.env`. Но указание ключа в `.env` ускорит инициализацию.

### Шаг 5: Перезапустить серверы

После настройки перезапустите:
- Django сервер
- Frontend dev сервер (если запущен)

## 🧪 Проверка работы

### 1. Проверить, что ключи загружены

В Django shell:

```python
from django.conf import settings
print("Public Key:", settings.VAPID_PUBLIC_KEY[:50] + "...")
print("Private Key:", "***" if settings.VAPID_PRIVATE_KEY else "NOT SET")
```

### 2. Проверить API endpoint

Откройте в браузере или через curl:

```bash
curl http://localhost:8000/api/v1/auth/vapid-public-key/
```

Должен вернуться JSON с публичным ключом:

```json
{
  "public_key": "..."
}
```

### 3. Проверить подписку на push-уведомления

1. Войдите в приложение
2. Откройте DevTools (F12)
3. Перейдите на вкладку Console
4. Должно появиться сообщение о подписке на push-уведомления
5. При первом подписании браузер попросит разрешение на уведомления

### 4. Проверить Service Worker

1. DevTools → Application → Service Workers
2. Должен быть активный Service Worker
3. Статус: `activated and is running`

## 🚀 Отправка тестового push-уведомления

Создайте Django management команду для тестовой отправки:

```python
# projects/management/commands/test_push.py
from django.core.management.base import BaseCommand
from django.contrib.auth import get_user_model
from users.push_utils import send_push_notification

class Command(BaseCommand):
    help = 'Отправить тестовое push-уведомление пользователю'

    def add_arguments(self, parser):
        parser.add_argument('username', type=str, help='Username пользователя')

    def handle(self, *args, **options):
        User = get_user_model()
        try:
            user = User.objects.get(username=options['username'])
            success = send_push_notification(
                user=user,
                title='Тестовое уведомление',
                body='Это тестовое push-уведомление!',
                url='/notifications'
            )
            if success:
                self.stdout.write(self.style.SUCCESS('Уведомление отправлено!'))
            else:
                self.stdout.write(self.style.WARNING('Не удалось отправить уведомление'))
        except User.DoesNotExist:
            self.stdout.write(self.style.ERROR(f'Пользователь {options["username"]} не найден'))
```

Запуск:

```bash
python manage.py test_push username
```

## 🔧 Использование в коде

### Отправка push-уведомления одному пользователю

```python
from users.push_utils import send_push_notification

send_push_notification(
    user=user,
    title='Новая рекламация',
    body=f'Вам назначена рекламация #{complaint.id}',
    url=f'/complaints/{complaint.id}',
    tag=f'complaint-{complaint.id}',
)
```

### Отправка нескольким пользователям

```python
from users.push_utils import send_push_to_multiple_users
from django.contrib.auth import get_user_model

User = get_user_model()
managers = User.objects.filter(role='manager')

send_push_to_multiple_users(
    users=managers,
    title='Новые задачи',
    body='У вас появились новые задачи',
    url='/dashboard',
)
```

## ⚠️ Частые проблемы

### Ошибка: "InvalidAccessError: The provided applicationServerKey is not valid"

**Причина:** VAPID ключ не настроен или невалиден.

**Решение:**
1. Проверьте, что ключи добавлены в `.env`
2. Убедитесь, что ключи сгенерированы корректно
3. Перезапустите сервер

### Ошибка: "VAPID публичный ключ не настроен"

**Причина:** Ключи не загружены из `.env`.

**Решение:**
1. Проверьте наличие `.env` файла
2. Проверьте формат ключей (без пробелов, переносов строк)
3. Проверьте, что `python-dotenv` установлен

### Push-уведомления не приходят

**Проверьте:**
1. Разрешение на уведомления выдано браузером
2. Service Worker активен
3. Подписка создана (проверьте в консоли)
4. Подписка сохранена на сервере (проверьте в админке Django)
5. VAPID ключи корректны

### Ошибка на сервере: "Invalid VAPID key"

**Причина:** Приватный ключ невалиден или неправильно закодирован.

**Решение:**
1. Перегенерируйте ключи заново
2. Убедитесь, что используете base64url формат
3. Проверьте, что нет лишних пробелов в `.env`

## 📚 Дополнительные ресурсы

- [Web Push Protocol](https://tools.ietf.org/html/rfc8030)
- [VAPID Specification](https://tools.ietf.org/html/rfc8292)
- [pywebpush Documentation](https://github.com/web-push-libs/pywebpush)

## 🔒 Безопасность

1. **Никогда не коммитьте приватный ключ** в репозиторий
2. Используйте разные ключи для dev и production окружений
3. Храните `.env` файл в безопасности
4. Регулярно ротируйте ключи (если нужно)

## ✅ Чеклист настройки

- [ ] Установлены зависимости (`pip install -r requirements.txt`)
- [ ] Ключи сгенерированы (`python generate_vapid_keys.py`)
- [ ] Ключи добавлены в `.env` файл
- [ ] `VAPID_CLAIM_EMAIL` настроен
- [ ] Серверы перезапущены
- [ ] API endpoint возвращает публичный ключ
- [ ] Push-подписка работает в браузере
- [ ] Тестовое уведомление отправлено успешно

